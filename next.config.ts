/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: "export",
  images: {
    domains: ["images.pexels.com", "master.automark.site", "flagcdn.com"],
  },
};

module.exports = nextConfig;
